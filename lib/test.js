
const { combinedCredentials } = require('./support/credon.js');

async function main() {
  
    const credentials = combinedCredentials[0];
    // console.log('Using credentials:', credentials);

    }

main();