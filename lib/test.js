
const { getCredentials } = require('./support/credon.js');

async function getcredentials() {
  
    const credentials = await main();
    // console.log('Using credentials:', credentials);

    }

runWebSocket();